val timeSpinner = new JSpinner(new SpinnerDateModel())
    val timeEditor = new JSpinner.DateEditor(timeSpinner, "HH:mm")
    timeSpinner.setEditor(timeEditor)
    val cal = Calendar.getInstance()
    cal.set(Calendar.HOUR_OF_DAY, x.map(_.getHour).getOrElse(14))
    cal.set(Calendar.MINUTE, x.map(_.getMinute).getOrElse(0))
    timeSpinner.setValue(cal.getTime)
    
cal.setTime(timeSpinner.getValue.asInstanceOf[java.util.Date])
x.setHour(cal.get(Calendar.HOUR_OF_DAY)).setMinute(cal.get(Calendar.MINUTE))
    
